<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= role.charAt(0).toUpperCase() + role.slice(1) %> Announcements</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        :root {
            --primary: #5e60ce;
            --secondary: #4835d4;
            --success: #48cae4;
            --light: #f9f9fb;
            --dark: #1a1a2e;
            --gray: #6b7280;
            --light-gray: #e5e7eb;
            --border-radius: 12px;
            --box-shadow: 0 6px 12px rgba(0, 0, 0, 0.08);
            --transition: all 0.3s ease-in-out;
            --gradient: linear-gradient(135deg, #5e60ce, #48cae4);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        body {
            background-color: var(--light);
            color: var(--dark);
            line-height: 1.7;
            font-size: 16px;
        }

        .container {
            margin-left: 250px;
            padding: 40px;
            min-height: 100vh;
            background: linear-gradient(180deg, #f0f2ff 0%, #f9f9fb 100%);
        }

        .page-title {
            color: var(--dark);
            font-size: 32px;
            margin-bottom: 30px;
            font-weight: 700;
            display: flex;
            align-items: center;
            gap: 12px;
            position: relative;
        }

        .page-title i {
            color: var(--primary);
            font-size: 28px;
        }

        .page-title::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 0;
            width: 60px;
            height: 4px;
            background: var(--gradient);
            border-radius: 2px;
        }

        .card {
            background-color: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            padding: 30px;
            margin-bottom: 40px;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 4px;
            background: var(--gradient);
            opacity: 0.7;
        }

        .card:hover {
            box-shadow: 0 12px 24px rgba(0, 0, 0, 0.1);
            transform: translateY(-4px);
        }

        .card-title {
            color: var(--dark);
            font-size: 22px;
            margin-bottom: 25px;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .card-title i {
            color: var(--primary);
            font-size: 20px;
        }

        .form-group {
            margin-bottom: 25px;
        }

        label {
            display: flex;
            align-items: center;
            gap: 6px;
            color: var(--dark);
            margin-bottom: 10px;
            font-weight: 500;
            font-size: 15px;
            letter-spacing: 0.02em;
        }

        input, textarea {
            width: 100%;
            padding: 14px 16px;
            border: 1px solid var(--light-gray);
            border-radius: var(--border-radius);
            font-size: 15px;
            background-color: #f8fafc;
            transition: var(--transition);
        }

        input:focus, textarea:focus {
            outline: none;
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(94, 96, 206, 0.15);
            background-color: white;
        }

        textarea {
            min-height: 160px;
            resize: vertical;
        }

        .btn {
            padding: 12px 28px;
            background: var(--gradient);
            color: white;
            border: none;
            border-radius: var(--border-radius);
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
            display: inline-flex;
            align-items: center;
            gap: 8px;
            position: relative;
            overflow: hidden;
        }

        .btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
            transition: 0.5s;
        }

        .btn:hover::before {
            left: 100%;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 12px rgba(94, 96, 206, 0.3);
        }

        .announcement-list {
            list-style-type: none;
        }

        .announcement-item {
            margin-bottom: 30px;
            padding: 20px;
            border-radius: 8px;
            background-color: #f8fafc;
            transition: var(--transition);
        }

        .announcement-item:hover {
            background-color: #f1f5f9;
            transform: translateX(5px);
        }

        .announcement-item:last-child {
            margin-bottom: 0;
        }

        .announcement-title {
            color: var(--primary);
            font-size: 19px;
            margin-bottom: 12px;
            font-weight: 600;
        }

        .announcement-content {
            color: var(--dark);
            margin-bottom: 15px;
            line-height: 1.8;
            font-size: 15px;
        }

        .announcement-meta {
            color: var(--gray);
            font-size: 14px;
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .meta-item i {
            color: var(--primary);
        }

        .empty-state {
            text-align: center;
            padding: 50px 0;
            color: var(--gray);
        }

        .empty-state i {
            font-size: 56px;
            margin-bottom: 20px;
            color: var(--light-gray);
            opacity: 0.7;
        }

        .empty-state p {
            font-size: 18px;
            font-weight: 500;
        }

        @media (max-width: 992px) {
            .container {
                margin-left: 0;
                padding: 25px;
            }
        }

        @media (max-width: 576px) {
            .announcement-meta {
                flex-direction: column;
                align-items: flex-start;
                gap: 8px;
            }

            .card {
                padding: 20px;
            }

            .page-title {
                font-size: 28px;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/clerksidebar') %>

    <div class="container">
        <h1 class="page-title">
            <i class="fas fa-bullhorn"></i>
            <%= role.charAt(0).toUpperCase() + role.slice(1) %> Announcements
        </h1>

        <!-- Create Announcement Section -->
        <div class="card">
            <h2 class="card-title">
                <i class="fas fa-plus-circle"></i>
                Create New Announcement
            </h2>
            <form action="/clerk/announcements" method="POST">
                <div class="form-group">
                    <label for="title">
                        <i class="fas fa-heading"></i>
                        Title
                    </label>
                    <input type="text" id="title" name="title" required placeholder="Enter announcement title">
                </div>
                <div class="form-group">
                    <label for="content">
                        <i class="fas fa-align-left"></i>
                        Content
                    </label>
                    <textarea id="content" name="content" required placeholder="Type your announcement content here..."></textarea>
                </div>
                <button type="submit" class="btn">
                    <i class="fas fa-paper-plane"></i>
                    Publish Announcement
                </button>
            </form>
        </div>

        <!-- Announcement List Section -->
        <div class="card">
            <h2 class="card-title">
                <i class="fas fa-list"></i>
                Announcement List
            </h2>
            <% if (announcements.length > 0) { %>
                <ul class="announcement-list">
                    <% announcements.forEach(ann => { %>
                        <li class="announcement-item">
                            <h3 class="announcement-title"><%= ann.title %></h3>
                            <p class="announcement-content"><%= ann.content %></p>
                            <div class="announcement-meta">
                                <span class="meta-item">
                                    <i class="fas fa-user"></i>
                                    Posted by: <%= ann.created_by %>
                                </span>
                                <span class="meta-item">
                                    <i class="fas fa-calendar-alt"></i>
                                    <%= ann.created_at.toLocaleString() %>
                                </span>
                            </div>
                        </li>
                    <% }) %>
                </ul>
            <% } else { %>
                <div class="empty-state">
                    <i class="fas fa-inbox"></i>
                    <p>No announcements available yet.</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        // Simple animation for form submission
        document.querySelector('form').addEventListener('submit', function(e) {
            const btn = this.querySelector('button[type="submit"]');
            btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Publishing...';
            btn.disabled = true;
        });
    </script>
</body>
</html>